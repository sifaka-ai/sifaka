"""
Validation components for ensuring LLM outputs meet requirements.
"""

# Import validator implementations
# These imports register the validators with the registry
from sifaka.validators import length_validator

# Re-export the length validator for convenience
from sifaka.validators.length_validator import LengthValidator, create_length_validator

# Convenience function
def length(min_length=None, max_length=None, unit="words"):
    """Create a length validator.
    
    Args:
        min_length: Minimum length (inclusive).
        max_length: Maximum length (inclusive).
        unit: Unit of measurement ("chars", "words", or "tokens").
        
    Returns:
        A length validator.
    """
    return create_length_validator(min_length, max_length, unit)

__all__ = [
    "LengthValidator",
    "create_length_validator",
    "length",
]
